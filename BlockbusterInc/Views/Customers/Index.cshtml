@model List<BlockbusterInc.Models.Customer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="mb-lg-4">Customer List</h2>

@if (Model.Count == 0)
{
    <text>There are no customers registered yet.</text>
}
else
{
    <table class="table table-bordered table-hover table-striped">
        <thead class="thead-dark">
            <tr>
                <th>Name</th>
                <th>Movies Rented</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var customer in Model)
            {
                <tr>
                    <td>@Html.ActionLink(customer.Name, "Details", "Customers", new { id = customer.Id }, null)</td>
                    <td>@customer.MoviesRented</td>
                </tr>
            }
        </tbody>
    </table>
}
